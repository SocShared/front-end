spring:
  zipkin:
    base-url: https://zipkin.socshared.ml/
    service:
      name: ${spring.application.name}
  main:
    allow-bean-definition-overriding: true
  thymeleaf:
    cache: false

feign:
  url:
    api: ${GATEWAY_URI:localhost:8081}
    auth: https://auth.socshared.ml
